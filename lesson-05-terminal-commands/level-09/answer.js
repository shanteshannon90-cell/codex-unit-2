// Level 09 — Practice: Navigate and run
// TODO: Export an object with a steps array: { steps: ["ls","cd folder","node script.js"] }
export default {
  steps: [
    "used ls to see list inside directory",
    "cd folder to select the directory within the folder",
    "added a script and node called the message",
  ],
};
