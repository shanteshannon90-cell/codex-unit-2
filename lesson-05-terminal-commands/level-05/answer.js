// Level 05 — Parent directory (..)
// TODO: Use `cd ..` and/or `../` to reference sibling files; describe the steps here.
export default "used pwd to fin main path, used ls to see all path directories, used cd to select directory in main directory then used cd .. / to return to the parent directory";
